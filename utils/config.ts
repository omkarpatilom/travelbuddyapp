export const Config = {
  // Set this to true to enable Mock Google Auth sheet in development.
  // Set to false (default) to run the real production-ready Google OAuth 2.0 flow.
  USE_MOCK_GOOGLE_AUTH: false,
};
