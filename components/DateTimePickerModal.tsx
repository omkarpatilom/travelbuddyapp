// Native (iOS/Android): the regular modal picker. The web build resolves
// DateTimePickerModal.web.tsx instead, because the native picker it wraps
// (@react-native-community/datetimepicker) has no web implementation.
export { default } from 'react-native-modal-datetime-picker';
